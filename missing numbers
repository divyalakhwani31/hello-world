#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include<bits/stdc++.h>
using namespace std;


int main(){
    int n,m;
    cin>>n;
    int a[n];
    int ca[999999];
    memset(ca,0,sizeof(ca));
    for(int i=0;i<n;++i){
        cin>>a[i];
        ca[a[i]]++;
    }
    sort(a,a+n);
    cin>>m;
    int b[m];
    int cb[999999];
    memset(cb,0,sizeof(cb));
    for(int i=0;i<m;++i){
        cin>>b[i];
        cb[b[i]]++;
    }
    sort(b,b+n);
    int diff[m-n];
    int x=0;
    
        for(int i=0;i<m;++i){
            if(b[i]!=b[i-1])
            if(ca[b[i]]<cb[b[i]])
                for(int k=0;k<(cb[b[i]]-ca[b[i]]);++k)
                    diff[x++]=b[i];
        }
    sort(diff,diff+m-n);
    for(int i=0;i<(m-n);++i)
        if(diff[i]!=diff[i-1])
        cout<<diff[i]<<" ";
    
    
    return 0;
}
